# ironic-ineptocracy-site

Static site. See `README-DEPLOY.md` for deployment.

## Knowledge

- Before starting, read the relevant pages from the `dillon-os` vault —
  `entities/`, `concepts/`, and `03_Content/` — starting at its `INDEX.md`
  (locally `~/dillon-os`, or checked out alongside this repo).
- Ground every claim about our business, audience, or voice in a vault page,
  and cite the page.
- If the vault isn't available in this session, say so instead of guessing.
